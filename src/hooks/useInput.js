/*
import {useState} from "react";

export const useInput = () => {
    const [query, setValue] = useState('Cooking');

    const onChange = event => {
        setValue(event.target.value)
    };

    return {
        query, onChange
    }

};*/
